<snippet>
	<content><![CDATA[

vector<string> takeUnkownInput(){
    vector<string> v;
    string s;
    string data;
    getline(cin, s);
    istringstream iss(s);
    while (iss >> data)
          v.push_back(data);

    return v;
}

]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>unknown size string input</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<scope>source.c++</scope>
</snippet>
